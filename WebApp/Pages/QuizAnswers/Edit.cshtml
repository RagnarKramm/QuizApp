@page
@model WebApp.Pages.QuizAnswers.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>QuizAnswer</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="QuizAnswer!.AnsweredQuizId" class="control-label"></label>
                <select asp-for="QuizAnswer!.AnsweredQuizId" class="form-control" asp-items="ViewBag.AnsweredQuizId"></select>
                <span asp-validation-for="QuizAnswer!.AnsweredQuizId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="QuizAnswer!.AnswerId" class="control-label"></label>
                <select asp-for="QuizAnswer!.AnswerId" class="form-control" asp-items="ViewBag.AnswerId"></select>
                <span asp-validation-for="QuizAnswer!.AnswerId" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="QuizAnswer!.Id" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
